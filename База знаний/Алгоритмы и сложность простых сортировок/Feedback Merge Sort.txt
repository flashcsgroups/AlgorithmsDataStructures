4. Сортировка слиянием (Merge Sort).
    /// Алгоритм (разделяй и властвуй):
    /// * Разделяем массив на n отсортированных массивов.
    /// * Собираем из n один массив по принципу сравнения элементов.
    ///   Берется элемент из 1-го и сравнивается с элементов 2-го,
    ///   Меньший элемент вставляется в 3-й массив, а оставшиеся продолжают сравнения.
    ///   Так продолжаем пока не будет сформирован 3-й массив.
    /// * И так слево направо продолжаем сливать массивы, пока не соберем итоговый.  
    
    /// Инвариант: массив из одного элемента считатается отсортированным.